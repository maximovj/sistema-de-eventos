name: Frontend CI

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/ui/**'
      - 'packages/shared-types/**'
      - 'package.json'
      - 'package-lock.json'
      - 'turbo.json'
  pull_request:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/ui/**'
      - 'packages/shared-types/**'
      - 'package.json'
      - 'package-lock.json'
      - 'turbo.json'

jobs:
  build:
    name: Build Angular App
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup npm
        run: npm install --global npm@11.17.0

      - name: Verify toolchain
        run: node --version && npm --version

      - name: Install dependencies
        run: npm ci

      - name: Check Shared types
        run: npm run check-types --workspace=@repo/shared-types

      - name: Build Services
        run: npm run build --workspace=@repo/shared-services

      - name: Build UI
        run: npm run build --workspace=@repo/ui

      - name: Build Web
        run: npm run build --workspace=web

      - name: Check application types
        run: npx turbo run check-types --filter=web

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: angular-dist
          path: apps/web/dist/web/
          if-no-files-found: error
